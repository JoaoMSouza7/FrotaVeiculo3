@model IEnumerable<FrotaPim.Web.Models.ViewsModels.UserViewModel>
@{
    ViewData["Title"] = "Usuários Cadastrados";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row header">
    <div class="col-md-12">
        <h3>Usuários</h3>
        <a href="/User/Criar" class="btn btn-primary btn-sm"><i class="fas fa-plus mx-2"></i>Novo Usuário</a>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table table-hover">
            <thead>
                <th>
                    Email
                </th>
                <th>
                    
                </th>
            </thead>
            <tbody>
                @if(@Model != null)
                {
                    foreach(var viewModel in @Model)
                    {
                        <tr>
                            <td>
                                <a class="name">@viewModel.Email</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>